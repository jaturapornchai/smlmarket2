import 'package:flutter/material.dart';

/// Simple test widget to show Thai date formatting
class DateTestWidget extends StatelessWidget {
  const DateTestWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('ทดสอบ Thai Date ใน Debug Console'));
  }
}
